name: Build

on:
  push:
    branches:
      - main
    tags:
      - "v*.*.*"
    paths-ignore:
      - '**.md'

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: beammp_manager_ubuntu.zip
            asset_name: beammp_manager_ubuntu
          - os: windows-latest
            artifact_name: beammp_manager_windows.zip
            asset_name: beammp_manager_windows
    runs-on: ${{ matrix.os }}

    env:
      MAIN_PY_FILE: 'src/run.py'

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: 3.13.x

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install the project
        run: uv sync --all-extras --dev

      - name: Build executable directory
        run: uv run pyinstaller --name beammp_manager --add-data "static:static" --add-data "templates:templates" ${{ env.MAIN_PY_FILE }}

      - name: Add example .env file
        run: cp .env.example dist/beammp_manager

      - name: Zip directory
        working-directory: dist/beammp_manager
        run: |
          7z a ${{ matrix.artifact_name }} *
          7z a ${{ matrix.artifact_name }} .env.example

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/beammp_manager/${{ matrix.artifact_name }}
          retention-days: 15
          overwrite: true
          archive: false
      
      - name: Get current release
        if: startsWith(github.ref, 'refs/tags/')
        id: get_current_release
        continue-on-error: true
        uses: joutvhu/get-release@v1
        with:
          tag_name: ${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload to new release
        if: startsWith(github.ref, 'refs/tags/') && steps.get_current_release.outcome == 'failure'
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: dist/beammp_manager/${{ matrix.artifact_name }}
          asset_name: ${{ matrix.asset_name }}_${{ github.ref_name }}.zip
          tag: ${{ github.ref }}
          draft: true
          overwrite: true

      - name: Upload to existing release
        if: startsWith(github.ref, 'refs/tags/') && steps.get_current_release.outcome == 'success'
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: dist/beammp_manager/${{ matrix.artifact_name }}
          asset_name: ${{ matrix.asset_name }}_${{ github.ref_name }}.zip
          tag: ${{ github.ref }}
          release_id: ${{ steps.get_current_release.outputs.id }}
          draft: true
          overwrite: true
